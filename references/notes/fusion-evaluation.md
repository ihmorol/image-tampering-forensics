# Fusion & Evaluation Literature Notes

Notes taken directly from local PDFs under `references/papers/`. Written incrementally, one paper at a time, appended to disk as each is finished. No network sources used except where explicitly noted (dataset webpage check). Any figure without a page/section citation was not verified from retrieved text.

---
